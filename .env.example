# RevenueCat public SDK keys are bundled into the app and are not secrets.
# Use a Test Store key locally, then override this same variable with the iOS
# app-specific public key (appl_...) for TestFlight and App Store builds.
EXPO_PUBLIC_REVENUECAT_API_KEY=test_your_test_store_key
